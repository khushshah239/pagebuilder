# Production environment — Vercel / AWS deployment
# Secrets (CDS_USERNAME, CDS_PASSWORD) are set as platform environment variables — never commit them.

NEXT_PUBLIC_CDS_BASE_URL=https://cds-beta.thepublive.com
NEXT_PUBLIC_CDS_PUBLISHER_ID=4027

NEXT_PUBLIC_PUBLISHER_KEY=crictoday

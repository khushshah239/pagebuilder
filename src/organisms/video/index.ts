// Barrel export for all Video Page organisms.
// Import from "@/organisms/video" instead of reaching into individual files.
// Prop interfaces are imported directly from "@/types/video/organism.types".
// Note: videobody maps to ArticleBody (same props) — no separate export needed.

export { VideoHero } from "./VideoHero";
export { VideoHeader } from "./VideoHeader";

"use client";

/**
 * Scroll reveal is handled entirely by CSS scroll-driven animations
 * (globals.css — @supports animation-timeline: view()).
 * This component is intentionally a no-op; kept so existing imports compile.
 */
export function ScrollReveal() {
  return null;
}
